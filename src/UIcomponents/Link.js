import styled from 'styled-components';

const Link = styled.a`
  color: #0077cc;
  text-decoration: none;
  font-weight: bold;
  &:hover {
    text-decoration: underline;
  }
`;

export default Link;
